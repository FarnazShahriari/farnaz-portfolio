# Specs

Concrete, build-ready specs. This is what Claude Code implements directly.

- One file per feature or page: `feature-name.md`
- Include: what it does, inputs/states, edge cases, and which decision(s) it
  implements (link to `../decisions/`).
- Keep it implementation-level, not strategic — the "why" belongs in decisions.
